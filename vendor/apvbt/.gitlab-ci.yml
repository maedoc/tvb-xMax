test:
  tags:
    - docker-runner
  image: ghcr.io/astral-sh/uv:debian
  cache:
    - paths:
        - hcp.zip
        - 1kb.zip
        - both.pkl 
  script:
    - uv venv /env
    - source /env/bin/activate
    - uv pip install matplotlib tqdm vbjax sbi typed-argparse
    - ./run.sh
